# Configurations and scripts

Store small, reusable examples here after review. Remove passwords, tokens, private keys, internal identifiers, and other sensitive values. Replace sensitive values with clear placeholders. Explain prerequisites and how each example was validated.

Never rely on `.gitignore` alone to catch secrets.
