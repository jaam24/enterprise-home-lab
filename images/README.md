# Images and screenshots

Add selected evidence to topic folders as work is documented, for example `hardware/`, `architecture/`, `networking/`, and `screenshots/`.

Use descriptive filenames such as `hp-z2-mini-bios-virtualization.jpg`. Link each image from the relevant document with a caption or surrounding explanation. Remove serial numbers, public IPs, usernames, and secrets before committing.
